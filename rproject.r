{"metadata":{"kernelspec":{"name":"ir","display_name":"R","language":"R"},"language_info":{"name":"R","codemirror_mode":"r","pygments_lexer":"r","mimetype":"text/x-r-source","file_extension":".r","version":"4.0.5"}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"data <- read.csv('/kaggle/input/tryagain/Before.csv')\n#data2 <- read.csv('/kaggle/input/kaggledata2/Before_R_defore_dummy.csv')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"install.packages('rsq')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"### 数据处理\n\n#### 加入交叉项","metadata":{}},{"cell_type":"code","source":"data$Com8 <- data$TotRmsAbvGrd * data$BedroomAbvGr #0.66\ndata$Com1 <- data$TotRmsAbvGrd * data$ndFlrSF \ndata$Com2 <- data$BedroomAbvGr * data$ndFlrSF\ndata$Com3 <- data$GarageArea * data$Bath_total\ndata$Com4 <- data$LotArea * data$LotFrontage\ndata$Com5 <- data$OverallQual * data$Bath_total\ndata$Com6 <- data$TotRmsAbvGrd * data$Bath_total\ndata$Com7 <- data$MSSubClass * data$ndFlrSF","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"data$MSSubClass","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"data$X <- NULL\n\ntrain <- data[which(data$lable==1),]\n\nlibrary(caTools)\nlibrary(matrixStats)\n\ntrain_data <- data[which(data$lable==1),]\ntest_data <- data[which(data$lable==0),]\ntrain_data <- train_data[, -which(colnames(train_data)=='lable')]\ntest_data <- test_data[, -which(colnames(train_data)=='lable')]\n\n#重新导入数据的话记得删1st,2nd\n\nnum <- c('MSSubClass', 'LotFrontage', 'LotArea', 'OverallQual', 'OverallCond', 'MasVnrArea', 'stFlrSF',\n         'ndFlrSF', 'BedroomAbvGr', 'KitchenAbvGr', 'TotRmsAbvGrd', 'Fireplaces', 'GarageArea', 'WoodDeckSF', 'PoolArea', 'MiscVal', 'UnfinishedBsm_ratio', 'LowQuality_ratio', 'Bath_total', 'Porch', 'Com1', 'Com2', 'Com3', 'Com4', 'Com5', 'Com6', 'Com7', 'Com8')\n# 标准化\n#train_data[,num] <- scale(train_data[, num], center = TRUE, scale = TRUE)\n#train_data <- as.data.frame(train_data)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"sqrt(diag(cov(train_data[,num])))","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"#train_data2 <- data2[which(data2$lable==1),]\n#test_data2 <- data2[which(data2$lable==0),]\n#train_data2 <- train_data2[, -which(colnames(train_data2)=='lable')]\n#test_data2 <- test_data2[, -which(colnames(train_data2)=='lable')]","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"qqnorm(train_data[,'SalePrice'])\nqqline(train_data[,'SalePrice'], col = 'red')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"qqnorm(log(train_data[,'SalePrice']))\nqqline(log(train_data[,'SalePrice']), col = 'red')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"### 模型初步建立","metadata":{}},{"cell_type":"code","source":"library(MASS)\n\nmodel <- lm(log(SalePrice) ~ .,data = train_data)\nstp <- stepAIC(model, direction='both', trace = F)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"print('AIC of Base Modle:')\nAIC(model)\nprint('BIC of Base Modle:')\nBIC(model)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"summary(model)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"summary(stp)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"print('AIC of model1:')\nAIC(stp)\nprint('BIC of model1:')\nBIC(stp)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"stp2 <- stepAIC(model, direction='both', trace = F, k = log(dim(train_data)[1]))","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"summary(stp2)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"print('AIC of model2:')\nAIC(stp2)\nprint('BIC of model2:')\nBIC(stp2)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"names <- c('SalePrice', 'MSSubClass', 'LotArea',  'OverallQual', \n    'OverallCond', 'stFlrSF', 'ndFlrSF', 'BedroomAbvGr', 'TotRmsAbvGrd',\n    'Fireplaces' , 'WoodDeckSF' , 'YearBuilt_Before1950' , 'YearBuilt_After1980' ,\n    'UnfinishedBsm_ratio', 'Bath_total', 'Porch' ,'MSZoning_FV', 'MSZoning_RH', 'MSZoning_RL', \n    'MSZoning_RM', 'LotConfig_CulDSac', 'MasVnrType_Stone', 'MasVnrType_BrkFace', 'MasVnrType_None',\n    'LotConfig_FR2','LotConfig_FR3','LotConfig_Inside','LandSlope_Sev','LandSlope_Mod',\n    'RoofMatl_CompShg','RoofMatl_Membran', 'RoofMatl_Metal', 'RoofMatl_Roll', \n    'RoofMatl_TarGrv', 'RoofMatl_WdShake', 'RoofMatl_WdShngl',\n    'ExterCond_Gd','ExterCond_Fa','ExterCond_TA',\n    'Foundation_PConc','Foundation_CBlock','Foundation_Stone','Foundation_Wood','BsmtExposure_Gd',\n    'BsmtExposure_Mn','BsmtExposure_No', 'PavedDrive_Y', 'PavedDrive_P',\n    'HeatingQC_Fa','HeatingQC_Gd','HeatingQC_Po','HeatingQC_TA','CentralAir_Y','KitchenQual_Gd','KitchenQual_Fa','KitchenQual_TA', \n    'Functional_Maj2','Functional_Min1','Functional_Min2','Functional_Mod','Functional_Typ','Functional_Sev',\n    'SaleCondition_Normal','SaleCondition_AdjLand','SaleCondition_Alloca','SaleCondition_Family','SaleCondition_Partial', \n    'Com2', 'Com3', 'Com6', 'Com7')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"好像咱处理的进去了很多，✌！\n\n'ExterCond_Po'删了,只有一个1","metadata":{}},{"cell_type":"code","source":"model1 <- lm(log(SalePrice) ~ .,data = train_data[,names])","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"summary(model1)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"print('AIC of model1:')\nAIC(model1)\nprint('BIC of model1:')\nBIC(model1)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"### 诊断","metadata":{}},{"cell_type":"markdown","source":"#### 残差分析\n\n1. 对所有模型中的变量画图（主要参照了第三章Residual那部分）\n\n* 看起来都线性的不行\n\n* 方差看着也都是常数\n\n* 离群值有，但不多且不明显，除了lotArea怪怪的（参照python）\n","metadata":{}},{"cell_type":"code","source":"for (i in 1:dim(model1$model)[1])\n    plot(model1$model[1:300,i],model1$residuals[1:300], main = colnames(model1$model)[i])","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"* 方差看着也确实是正态的","metadata":{}},{"cell_type":"code","source":"plot(model1$fitted.values, model1$residuals,\n     xlab = 'SalePrice',ylab = 'residuals',\n     main = 'Residual Plot')\nqqnorm(model1$residuals)\nqqline(model1$residuals,col = 'red')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"* 残差对所有被丢弃的变量画图\n\n结论是？？？扔得好？","metadata":{}},{"cell_type":"code","source":"for (i in 1:length(colnames(data))){\n    if (!(colnames(data)[i] %in% names))\n        plot(train_data[,i], model1$residuals, main = colnames(data)[i])\n}","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"使用未dummy的数据对分类变量进行整合画图","metadata":{}},{"cell_type":"code","source":"library(lattice)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"temp = cbind(as.data.frame(model1$residual),as.data.frame(train_data2$Functional))\ncolnames(temp) <- c('residual','Functional')\ntemp$Functional <- factor(temp$Functional)\nxyplot(residual~Functional,data=temp,col=\"black\",jitter.x = T,abline = 0,xlab = '',main='Functional')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"2. 齐方差性考察\n\n单变量方法，不用：\n\n* B-F Test\n\n* B-P Test : 要求独立且正态，不选用\n\n**画图看**","metadata":{}},{"cell_type":"code","source":"plot(model1$fitted.values, model1$residuals,\n     xlab = 'SalePrice',ylab = 'residuals',\n    main = 'Residual Plot')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"* 异方差性检验\n\nncvTest生成计分检验，原假设为误差方差不变，备择假设为误差方差随拟合值水平的变化而变化","metadata":{}},{"cell_type":"code","source":"library(lmtest)\nlibrary(car)\nncvTest(model1)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"**！！！结论是H1，我们的方差不是常数，需要修正！！！**","metadata":{}},{"cell_type":"markdown","source":"* F-Test Lack of Fit\n\n找不到参照的模型，不用","metadata":{}},{"cell_type":"markdown","source":"#### 离群值 （第十章）\n\n* https://stats.stackexchange.com/questions/175/how-should-outliers-be-dealt-with-in-linear-regression-analysis 提到了一个方法：rlm\n\n1. Studentized Deleted Residuals ---> Y的离群值\n\n判断标准：是否大于3","metadata":{}},{"cell_type":"code","source":"sdr <- rstudent(model1)\ntrain_data[which(abs(sdr)>3),'SalePrice']","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"#好像是为了考查Y离群的原因是不是周围的样本点太少，似乎没得出结论\ntrain_data[which(abs(train_data[,'SalePrice'] - 60000) <= 5000),names]","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"hist(train_data[,'SalePrice'], nclass = 60)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"2. Hat Matrix ---> X的离群值","metadata":{}},{"cell_type":"code","source":"summary(stp2)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"H <- hatvalues(stp2)\n\n#判断标准1\nh_mean <- 2*(48)/dim(train_data)[1]\nh1 <- as.data.frame(H[which(H > h_mean)])\nx_h <- rownames(h1)\nh_mean\nx_h","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"#判断标准2\nH[which(H > 0.5)]","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"3. DFFITS, Cook's Distance, DFBETAS ---> Influential Cases\n\n* DIFFITS:  X Outliers","metadata":{}},{"cell_type":"code","source":"DIFFITS <- dffits(stp2)\nd <- 2*sqrt(48/dim(train_data)[1])\nprint(d)\ndiff <- as.data.frame(DIFFITS[which(abs(DIFFITS) > d)])","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"x_out <- c()\nfor (i in 1:dim(diff)[1]){\n    if (rownames(diff)[i] %in% rownames(h1)){\n        x_out <- c(x_out, c(rownames(diff)[i]))\n    }\n}\nx_out","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"* Cook's Distance","metadata":{}},{"cell_type":"code","source":"COK <- cooks.distance(stp2)\nCOK[which(abs(COK) >= 0.2)]\n#大于0.5的才严重","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"* DFBETAS : 对回归参数的影响\n\n  第i组数据对第k个系数的影响","metadata":{}},{"cell_type":"code","source":"# 用于细化显著\nDFBETAS <- dfbetas(model1)\ndf <- 2/sqrt(dim(train_data)[1])\nprint(df)\n#which(abs(DFBETAS) > df)\nx_df <- DFBETAS[x_h, ]\nwhere <- which(abs(x_df) > df, arr.ind = TRUE)\nfor (i in 1:dim(where)[1]){\n    print(rownames(where)[i])\n    print(colnames(train_data)[where[i,2]])\n}\n","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"#### 多重共线性\n\nVIF","metadata":{}},{"cell_type":"code","source":"library(car)\nlibrary(carData)\nV <- vif(stp2)\nprint(\"VIF均值:\")\nsum(V)/length(V)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"print(V)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"#### Added-Variable Plots 检查模型质量\n\n已经用了step-wise,暂时不用这个","metadata":{}},{"cell_type":"markdown","source":"### 修复\n\n（第十一章）\n\n#### 1. 不具有齐方差性---> Weighted Least Squares","metadata":{}},{"cell_type":"code","source":"C <- t(cor(stp2$residuals^2, stp2$model))  #Residuals 和 X,Y的相关性矩阵\ne_model <- which(abs(C)>=0.05)\nwls_data <- stp2$model[e_model]","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"re_function <- lm(abs(stp2$residuals) ~., data = wls_data)\nmodel2 <- lm(log(SalePrice) ~ .,data = train_data[,names], weights = 1/(re_function$fitted.values)^2)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"a <- re_function$fitted.values\na[which(a<0)]\n###取了绝对值","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"plot(model2$fitted.values, model2$residuals,\n     xlab = 'SalePrice',ylab = 'residuals',\n    main = 'Residual Plot of WLS')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"13以上的数据很少，拟合效果很不稳定","metadata":{}},{"cell_type":"code","source":"write.csv(as.data.frame(stp$coef),'stp_coef.csv')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"write.csv(as.data.frame(model2$coef),'model2_coef.csv')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"ncvTest(model2)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"#### 2.多重共线性---> Ridge Regression","metadata":{}},{"cell_type":"code","source":"x <- train_data[,names]\ny <- log(train_data[,'SalePrice'])\nx$SalePrice <- NULL\nx <- as.matrix(x)\ncvfit <- cv.glmnet(x, y, weights = 1/(re_function$fitted.values)^2)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"print(cvfit$lambda.min)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"log(cvfit$lambda.min)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"library(glmnet)\nx <- train_data[,names]\ny <- log(train_data[,'SalePrice'])\nx$SalePrice <- NULL\nfit <- glmnet(x, y, weights = 1/(re_function$fitted.values)^2)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"plot(fit,xvar = 'lambda', xlim = c(-8,0))","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"plot(cvfit, xlab = 'Log Lambda')","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"predict(fit, newx = as.matrix(x),  s = cvfit$lambda.min)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"ROWNAME <- rownames(coef(fit, s = cvfit$lambda.min))\nrn_not_0 <- ROWNAME[which(coef(fit, s = cvfit$lambda.min)!=0)]","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"vars <- rn_not_0[2:length(rn_not_0)]\nvars[length(rn_not_0)] <- 'SalePrice'","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"try <- lm(log(SalePrice) ~ .,data = train_data[,vars])","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"V <- vif(try)\nsum(V)/length(V)\nmax(V)\nwhich(V>10)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"print('VIF均值为:')\nsum(V)/length(V)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"y_hat_log <- predict(fit, newx = as.matrix(x),  s = cvfit$lambda.min)\ne <- log(train_data[, 'SalePrice']) - y_hat_log\nSSE <- sum(e**2)\nSSTO <- sum((log(train_data[, 'SalePrice']) - mean(log(train_data[, 'SalePrice'])))**2)\n1 - SSE/SSTO ","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"plot(y_hat_log, e)","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"markdown","source":"#### 3.Influential Cases---> Robust Regression\n\n网上说如果有很多Outlier的化话用这个，同时不用R-square作为评价指标，但我们似乎没有很多？https://stackoverflow.com/questions/60073531/is-it-appropriate-to-calculate-r-squared-of-robust-regression-using-rlm","metadata":{}},{"cell_type":"code","source":"library(MASS)\nlibrary(rsq)\nrobust <- rlm(log(SalePrice) ~ .,data = train_data[,names], weights = 1/(re_function$fitted.values)^2)\nplot(robust$fitted.values, robust$residuals,\n     xlab = 'SalePrice',ylab = 'residuals',\n    main = 'Residual Plot of Robust Regression')\n#好像没啥区别啊 笑死 也许可以用划分后的data比较一下预测的效果","metadata":{"trusted":true},"execution_count":null,"outputs":[]},{"cell_type":"code","source":"","metadata":{},"execution_count":null,"outputs":[]}]}